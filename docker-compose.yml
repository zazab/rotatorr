services:
  rotatorr:
    image: zzazab/rotatorr:latest
    volumes:
      - ${CONFIG_FILE_PATH:-./config.local.json}:/config/config.json:ro
    ports:
      - "3000:3000"
    restart: unless-stopped
